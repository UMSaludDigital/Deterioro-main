using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class SeguimientoInstrucciones : MonoBehaviour
{
    public Transform memoryLocation;
    public Transform instructionsLocation;
    // Start is called before the first frame update
    void Start()
    {
        if (GameManager.Instance._tipo_juego==1)
        {
           
        }
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
